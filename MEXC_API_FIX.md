# Исправление MEXC API Signature Failed (602)

## ✅ Что было исправлено:

### 1. **MEXC API подпись согласно официальной документации**

Проблема: Использовался неправильный формат подписи
- ❌ Было: Параметры сортировались + подпись в body как `sign`
- ✅ Стало: Правильный формат по документации MEXC

### Правильный формат MEXC API:

#### Для POST запросов (создание ордеров, leverage):
```
Signature Payload = AccessKey + Timestamp + JSON Body
Signature = HMAC-SHA256(Signature Payload, Secret Key)

Headers:
- ApiKey: <your_api_key>
- Request-Time: <timestamp>
- Signature: <calculated_signature>
- Content-Type: application/json

Body: JSON (НЕ нужна сортировка для POST!)
```

#### Для GET запросов (баланс):
```
Signature Payload = AccessKey + Timestamp
Signature = HMAC-SHA256(Signature Payload, Secret Key)

Headers:
- ApiKey: <your_api_key>
- Request-Time: <timestamp>
- Signature: <calculated_signature>
```

### 2. **Добавлен детальный лист с ошибками в Excel**

В Excel отчете лист "Пропущенные возможности" теперь показывает:

| Причина | Описание |
|---------|----------|
| 💰 Недостаточно баланса | Недостаточно средств для открытия позиции |
| 📉 Текущая позиция не прибыльна | Не закрыли текущую позицию из-за убытка |
| 🚫 Нет свободных слотов | Достигнут maxOpenPositions |
| ⏱️ Rate limit | Уже создается другой ордер |
| ❌ Ошибка создания ордера | Биржа вернула ошибку |
| ⚠️ Размер позиции превышает лимит | Позиция > $100 (защита) |

## 🧪 Тестирование:

### 1. Проверка API (без ордеров):
```bash
node test-api.js
```

Должны увидеть:
```
✅ Binance Futures API - работает
   USDT баланс: 123.45

✅ MEXC Futures API - работает
   USDT баланс: 67.89
```

### 2. Запуск бота в тестовом режиме:
```bash
npm start
```

В [config.json](config.json) убедитесь:
```json
{
  "trading": {
    "testMode": true,        // TRUE для симуляции
    "maxOpenPositions": 1    // Только 1 позиция!
  }
}
```

### 3. Проверка после тестов:

После Ctrl+C проверьте Excel отчет в папке `reports/`:
- Лист "Пропущенные возможности" - все ошибки с причинами
- Лист "Сводка" - общая статистика
- Лист "Все сделки" - закрытые позиции

## 📚 Источники (документация MEXC):

- [MEXC Futures API Integration Guide](https://www.mexc.com/api-docs/futures/integration-guide)
- [MEXC Account and Trading Endpoints](https://www.mexc.com/api-docs/futures/account-and-trading-endpoints)

## ⚠️ Важные замечания:

1. **MEXC Futures API доступно только институциональным пользователям**
   - Если ваш аккаунт не institutional, получите ошибку 403/404
   - Обращайтесь: institution@mexc.com

2. **Timestamp должен быть в пределах 10 секунд от времени сервера**
   - Если часы компьютера отстают/спешат > 10 сек, будет ошибка

3. **API ключи должны быть для Futures, не Spot**

## 🔧 Если все еще ошибка 602:

1. Проверьте что API ключи правильные (скопировали без пробелов)
2. Убедитесь что ключи для **Futures Contract**, не Spot
3. Проверьте время на компьютере (должно совпадать с мировым)
4. Убедитесь что у вас institutional аккаунт на MEXC
5. Проверьте IP whitelist если настроен

## 🎯 Готово к тестированию!

Все исправления внесены. Запустите:
```bash
node test-api.js
```

Если оба теста пройдут (✅), можете запускать бота.
