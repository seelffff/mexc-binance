# ✅ API Validation Feature

## Описание

Перед началом торговли бот теперь **автоматически проверяет** работоспособность API ключей обеих бирж (Binance и MEXC). Это предотвращает запуск торговли с неправильными или недействительными ключами.

---

## Что проверяется?

### Для каждой биржи проверяется:

1. **Подключение (Connectivity)**
   - Ping endpoint биржи
   - Проверяет доступность серверов

2. **Аутентификация (Authentication)**
   - Запрос баланса с использованием API ключей
   - Проверяет правильность ключей и их права доступа

---

## Как это работает?

### При запуске бота (`npm start`):

```
═══════════════════════════════════════════
    Проверка API ключей бирж
═══════════════════════════════════════════

🔍 Проверка Binance Futures API...
  ✓ Подключение к Binance: OK
  ✓ API ключи Binance: OK
  ✓ USDT баланс: $1250.75

🔍 Проверка MEXC Futures API...
  ✓ Подключение к MEXC: OK
  ✓ API ключи MEXC: OK
  ✓ USDT баланс: $850.25

═══════════════════════════════════════════
✓ ВСЕ ПРОВЕРКИ ПРОЙДЕНЫ УСПЕШНО!
  Готово к торговле
═══════════════════════════════════════════

✅ Все API ключи работают корректно!
```

После успешной проверки бот запустит основную логику торговли.

---

## Что происходит при ошибках?

### ❌ Если проверка не пройдена:

```
═══════════════════════════════════════════
✗ ПРОВЕРКИ НЕ ПРОЙДЕНЫ!
  Исправьте ошибки перед запуском торговли

BINANCE: Неверная подпись API. Проверьте API ключи в .env файле
═══════════════════════════════════════════

❌ Торговля не может быть запущена из-за ошибок API
⚠ Проверьте конфигурацию и API ключи в .env
```

Бот **НЕ запустится** пока ошибки не будут исправлены.

---

## Возможные ошибки и решения

### Binance Errors:

| Код ошибки | Описание | Решение |
|------------|----------|---------|
| -1022 | Неверная подпись API | Проверьте `BINANCE_API_KEY` и `BINANCE_API_SECRET` в `.env` файле |
| -2014 | API ключ недействителен | Создайте новые API ключи на Binance |
| -2015 | API ключ не имеет прав на Futures Trading | При создании ключей включите "Enable Futures" |
| Connection failed | Не удалось подключиться к серверам | Проверьте интернет-соединение |

### MEXC Errors:

| Код ошибки | Описание | Решение |
|------------|----------|---------|
| 1002 | Contract not activated (institutional only) | Отключите MEXC в `config.json` (`"enabled": false`) или получите institutional access: institution@mexc.com |
| 602 | Неверная подпись API | Проверьте `MEXC_API_KEY` и `MEXC_API_SECRET` в `.env` файле |
| 600 | API ключ недействителен | Создайте новые API ключи на MEXC |
| Connection failed | Не удалось подключиться к серверам | Проверьте интернет-соединение |

---

## Отключение биржи в конфигурации

Если хотите торговать только на одной бирже, отключите другую в `config.json`:

```json
{
  "exchanges": {
    "binance": {
      "enabled": true
    },
    "mexc": {
      "enabled": false
    }
  }
}
```

При отключенной бирже:
- Проверка API ключей не выполняется
- Выводится предупреждение: `⚠ MEXC отключен в конфигурации`
- Бот запустится только с Binance

---

## Технические детали

### Файлы:
- **src/utils/api-validator.ts** - Класс ApiValidator для проверки API
- **src/main.ts** - Интеграция валидации перед запуском торговли
- **src/arbitrage-detector.ts** - Геттеры для доступа к exchange instances

### Методы:
- `ApiValidator.checkBinance()` - Проверка Binance API
- `ApiValidator.checkMexc()` - Проверка MEXC API
- `ApiValidator.validateAll()` - Проверка всех бирж с итоговым отчётом

### Endpoints:
- **Binance Ping**: `GET /fapi/v1/ping`
- **Binance Balance**: `GET /fapi/v2/balance` (signed)
- **MEXC Ping**: `GET /api/v1/contract/ping`
- **MEXC Balance**: `GET /api/v1/private/account/assets` (signed)

---

## Преимущества

✅ **Раннее обнаружение проблем** - Ошибки выявляются до начала торговли
✅ **Понятные сообщения об ошибках** - Детальное описание проблемы и решения
✅ **Защита от неправильных ключей** - Невозможно запустить торговлю с плохими API ключами
✅ **Проверка прав доступа** - Убеждается что ключи имеют права на Futures Trading
✅ **Отображение балансов** - Сразу показывает доступные средства на каждой бирже

---

## Быстрый старт

1. Убедитесь что `.env` заполнен правильно:
```bash
cat .env | grep API
```

Должно быть БЕЗ пробелов и кавычек:
```
BINANCE_API_KEY=your_key_here
BINANCE_API_SECRET=your_secret_here
MEXC_API_KEY=your_key_here
MEXC_API_SECRET=your_secret_here
```

2. Запустите бот:
```bash
npm start
```

3. Дождитесь результата проверки API ключей

4. Если все ✅ - бот начнёт торговлю
   Если есть ❌ - исправьте ошибки и запустите снова

---

## Готово! 🎯

Теперь бот **гарантированно** не запустится с неправильными API ключами!
