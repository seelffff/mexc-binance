# Исправление Rate Limit ошибок

## ✅ Что было исправлено:

### 1. **Rate Limiter добавлен в TradeExecutor**
- Минимальная задержка 2 секунды между попытками открыть позицию
- Блокировка одновременного создания нескольких ордеров
- Автоматическая задержка 500ms между LONG и SHORT ордерами

### 2. **Исправлена подпись Binance API**
- Параметры теперь правильно сортируются по алфавиту
- Подпись создается из корректно отформатированной строки

### 3. **Leverage установка теперь опциональна**
- Ошибки leverage больше не блокируют создание ордеров
- Логируются как warnings, но ордера создаются

## 🔧 Настройка для снижения нагрузки:

### В `config.json`:

```json
{
  "arbitrage": {
    "minSpreadPercent": 0.5,  // Увеличить чтобы было меньше арбитражей
    "enabled": true
  },
  "trading": {
    "testMode": true,          // TRUE для тестирования БЕЗ реальных денег
    "positionSizeUSD": 1,       // Начните с $1
    "maxOpenPositions": 1,      // ВАЖНО: Только 1 позиция одновременно!
    "positionTimeoutSeconds": 60
  }
}
```

### Ключевые параметры для снижения rate limit:

1. **maxOpenPositions: 1** - Открывать только 1 позицию одновременно
2. **minSpreadPercent: 0.5** - Меньше сработок = меньше запросов
3. **positionSizeUSD: 1** - Минимальный размер для тестов

## 📊 Логика Rate Limiter:

### Защита от "too frequent":
1. Перед созданием ордера проверяется - не создается ли уже другой ордер
2. Если с последнего ордера прошло меньше 2 секунд - бот ждет
3. Между LONG и SHORT ордерами задержка 500ms
4. Между закрытием LONG и SHORT тоже задержка 500ms

### Пример лога при работе rate limiter:
```
Rate limit: ждем 1500ms перед следующим ордером...
Пропускаем BTCUSDT - уже создается другой ордер
```

## 🎯 Рекомендации для тестирования:

### 1. Сначала тест в симуляции:
```json
"testMode": true,
"maxOpenPositions": 1
```

### 2. Затем с минимальным размером:
```json
"testMode": false,
"positionSizeUSD": 1,
"maxOpenPositions": 1
```

### 3. После успешных тестов увеличивайте:
```json
"positionSizeUSD": 5,
"maxOpenPositions": 2
```

## ⚠️ Важно:

- **MEXC имеет строгий rate limit** - не более 1 запроса в 500ms
- **Используйте maxOpenPositions: 1** для начала
- **Не уменьшайте minSpreadPercent ниже 0.3%** - будет слишком много сработок
- **Следите за логами** - если видите rate limit warnings, увеличьте задержки

## 🔍 Проверка API перед запуском:

```bash
node test-api.js
```

Этот скрипт проверит API ключи БЕЗ создания ордеров.
